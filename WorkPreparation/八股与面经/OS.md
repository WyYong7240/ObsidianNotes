# 01硬件结构篇

## 2.1 CPU执行程序的方式

- 最重要的是定义计算机基本结构为 5 个部分，分别是**运算器、控制器、存储器、输入设备、输出设备**，这 5 个部分也被称为**冯诺依曼模型**。

- CPU 内部还有一些组件，常见的有**寄存器、控制单元和逻辑运算单元**

- 常见的寄存器种类：

  - *通用寄存器*，用来存放需要进行运算的数据，比如需要进行加和运算的两个数据。
  - *程序计数器*，用来存储 CPU 要执行下一条指令「所在的内存地址」，注意不是存储了下一条要执行的指令，此时指令还在内存中，程序计数器只是存储了下一条指令「的地址」。
  - *指令寄存器*，用来存放当前正在执行的指令，也就是指令本身，指令被执行完成之前，指令都存储在这里。

- 总线是用于 CPU 和内存以及其他设备之间的通信，总线可分为 3 种：

  - *地址总线*，用于指定 CPU 将要操作的内存地址；
  - *数据总线*，用于读写内存的数据；
  - *控制总线*，用于发送和接收信号，比如中断、设备复位等信号，CPU 收到信号后自然进行响应，这时也需要控制总线；

  当 CPU 要读写内存数据的时候，一般需要通过下面这三个总线：

  - 首先要通过「地址总线」来指定内存的地址；
  - 然后通过「控制总线」控制是读或写命令；
  - 最后通过「数据总线」来传输数据；
  
- 那 CPU 执行程序的过程如下：

  - 第一步，CPU 读取「程序计数器」的值，这个值是指令的内存地址，然后 CPU 的「控制单元」操作「地址总线」指定需要访问的内存地址，接着通知内存设备准备数据，数据准备好后通过「数据总线」将指令数据传给 CPU，CPU 收到内存传来的数据后，将这个指令数据存入到「指令寄存器」。
  - 第二步，「程序计数器」的值自增，表示指向下一条指令。这个自增的大小，由 CPU 的位宽决定，比如 32 位的 CPU，指令是 4 个字节，需要 4 个内存地址存放，因此「程序计数器」的值会自增 4；
  - 第三步，CPU 分析「指令寄存器」中的指令，确定指令的类型和参数，如果是计算类型的指令，就把指令交给「逻辑运算单元」运算；如果是存储类型的指令，则交由「控制单元」执行；

- 现代大多数 CPU 都使用来流水线的方式来执行指令，所谓的流水线就是把一个任务拆分成多个小任务，于是一条指令通常分为 4 个阶段，称为 4 级流水线：

  1. CPU 通过程序计数器读取对应内存地址的指令，这个部分称为 **Fetch（取得指令）**；
  2. CPU 对指令进行解码，这个部分称为 **Decode（指令译码）**；
  3. CPU 执行指令，这个部分称为 **Execution（执行指令）**；
  4. CPU 将计算结果存回寄存器或者将寄存器的值存入内存，这个部分称为 **Store（数据回写）**；

  上面这 4 个阶段，我们称为**指令周期（\*Instrution Cycle\*）**，CPU 的工作就是一个周期接着一个周期，周而复始。

  > - 取指令的阶段，我们的指令是存放在**存储器**里的，实际上，通过程序计数器和指令寄存器取出指令的过程，是由**控制器**操作的；
  > - 指令的译码过程，也是由**控制器**进行的；
  > - 指令执行的过程，无论是进行算术操作、逻辑操作，还是进行数据传输、条件分支操作，都是由**算术逻辑单元**操作的，也就是由**运算器**处理的。但是如果是一个简单的无条件地址跳转，则是直接在**控制器**里面完成的，不需要用到运算器。
  
- 指令从功能角度划分，可以分为 5 大类：

  - *数据传输类型的指令*，比如 `store/load` 是寄存器与内存间数据传输的指令，`mov` 是将一个内存地址的数据移动到另一个内存地址的指令；
  - *运算类型的指令*，比如加减乘除、位运算、比较大小等等，它们最多只能处理两个寄存器中的数据；
  - *跳转类型的指令*，通过修改程序计数器的值来达到跳转执行指令的过程，比如编程中常见的 `if-else`、`switch-case`、函数调用等。
  - *信号类型的指令*，比如发生中断的指令 `trap`；
  - *闲置类型的指令*，比如指令 `nop`，执行后 CPU 会空转一个周期；

- 对于 CPU 时钟周期数我们可以进一步拆解成：「**指令数 x 每条指令的平均时钟周期数（\*Cycles Per Instruction\*，简称 `CPI`）**」，于是程序的 CPU 执行时间的公式可变成如下：

  $${程序的CPU执行时间=指令数量\times CPI \times 时钟周期时间}$$

  因此，要想程序跑的更快，优化这三者即可：

  - *指令数*，表示执行程序所需要多少条指令，以及哪些指令。这个层面是基本靠编译器来优化，毕竟同样的代码，在不同的编译器，编译出来的计算机指令会有各种不同的表示方式。
  - *每条指令的平均时钟周期数 CPI*，它的含义很简单：CPU 处理一批指令时，平均下来每条指令要花多少个时钟周期。现代 CPU 普遍采用的流水线技术（Pipeline），原理和工厂的流水线作业一模一样：不用等一条指令从头到尾完全执行完，再开始下一条，而是把指令的执行拆成多个步骤，让多条指令的不同步骤重叠并行执行。靠这个技术，CPU 就能把平均每条指令消耗的时钟周期数压到最低，单位时间内能处理的指令数大幅提升，性能也随之上涨。
  - *时钟周期时间*，表示计算机主频，取决于计算机硬件。有的 CPU 支持超频技术，打开了超频意味着把 CPU 内部的时钟给调快了，于是 CPU 工作速度就变快了，但是也是有代价的，CPU 跑的越快，散热的压力就会越大，CPU 会很容易奔溃。

# 04内存管理篇

## 虚拟内存

- 我们程序所使用的内存地址叫做**虚拟内存地址**（*Virtual Memory Address*）
- 实际存在硬件里面的空间地址叫**物理内存地址**（*Physical Memory Address*）。

操作系统引入了虚拟内存，进程持有的虚拟地址会通过 CPU 芯片中的内存管理单元（MMU）的映射关系，来转换变成物理地址，然后再通过物理地址访问内存

操作系统是如何管理虚拟地址与物理地址之间的关系？

主要有两种方式，分别是**内存分段和内存分页**，分段是比较早提出的，我们先来看看内存分段。

### 内存分段

程序是由若干个逻辑分段组成的，如可由代码分段、数据分段、栈段、堆段组成。**不同的段是有不同的属性的，所以就用分段（\*Segmentation\*）的形式把这些段分离出来。**

分段机制下，虚拟地址和物理地址是如何映射的？

分段机制下的虚拟地址由两部分组成，**段选择因子**和**段内偏移量**。

段选择因子和段内偏移量：

- **段选择子**就保存在段寄存器里面。段选择子里面最重要的是**段号**，用作段表的索引。**段表**里面保存的是这个**段的基地址、段的界限和特权等级**等。
- 虚拟地址中的**段内偏移量**应该位于 0 和段界限之间，如果段内偏移量是合法的，就将段基地址加上段内偏移量得到物理内存地址。

虚拟地址是通过**段表**与物理地址进行映射的，分段机制会把程序的虚拟地址分成 4 个段，每个段在段表中有一个项，在这一项找到段的基地址，再加上偏移量，于是就能找到物理内存中的地址：

<img src="https://cdn.xiaolincoding.com//mysql/other/c5e2ab63e6ee4c8db575f3c7c9c85962.png" style="zoom:80%;" />

分段的办法很好，解决了程序本身不需要关心具体的物理内存地址的问题，但它也有一些不足之处：

- 第一个就是**内存碎片**的问题。
- 第二个就是**内存交换的效率低**的问题。

接下来，说说为什么会有这两个问题。

我们先来看看，分段为什么会产生内存碎片的问题？

我们来看看这样一个例子。假设有 1G 的物理内存，用户执行了多个程序，其中：

- 游戏占用了 512MB 内存
- 浏览器占用了 128MB 内存
- 音乐占用了 256 MB 内存。

这个时候，如果我们关闭了浏览器，则空闲内存还有 1024 - 512 - 256 = 256MB。

如果这个 256MB 不是连续的，被分成了两段 128 MB 内存，这就会导致没有空间再打开一个 200MB 的程序。

<img src="https://cdn.xiaolincoding.com//mysql/other/6142bc3c917e4a6298bdb62936e0d332.png" style="zoom:80%;" />

内存分段会出现内存碎片吗？

内存碎片主要分为，内部内存碎片和外部内存碎片。

内存分段管理可以做到段根据实际需求分配内存，所以有多少需求就分配多大的段，所以**不会出现内部内存碎片**。

但是由于每个段的长度不固定，所以多个段未必能恰好使用所有的内存空间，会产生了多个不连续的小物理内存，导致新的程序无法被装载，所以**会出现外部内存碎片**的问题。

解决「外部内存碎片」的问题就是**内存交换**。

> 可以把音乐程序占用的那 256MB 内存写到硬盘上，然后再从硬盘上读回来到内存里。不过再读回的时候，我们不能装载回原来的位置，而是紧紧跟着那已经被占用了的 512MB 内存后面。这样就能空缺出连续的 256MB 空间，于是新的 200MB 程序就可以装载进来。
>
> 这个内存交换空间，在 Linux 系统里，也就是我们常看到的 Swap 空间，这块空间是从硬盘划分出来的，用于内存与硬盘的空间交换。
>
> 再来看看，分段为什么会导致内存交换效率低的问题？
>
> 对于多进程的系统来说，用分段的方式，外部内存碎片是很容易产生的，产生了外部内存碎片，那不得不重新 `Swap` 内存区域，这个过程会产生性能瓶颈。
>
> 因为硬盘的访问速度要比内存慢太多了，每一次内存交换，我们都需要把一大段连续的内存数据写到硬盘上。
>
> 所以，**如果内存交换的时候，交换的是一个占内存空间很大的程序，这样整个机器都会显得卡顿。**

### 内存分页

**分页是把整个虚拟和物理内存空间切成一段段固定尺寸的大小**。这样一个连续并且尺寸固定的内存空间，我们叫**页**（*Page*）。在 Linux 下，每一页的大小为 `4KB`。

虚拟地址与物理地址之间通过**页表**来映射，如下图：

<img src="https://cdn.xiaolincoding.com//mysql/other/08a8e315fedc4a858060db5cb4a654af.png" style="zoom:80%;" />

页表是存储在内存里的，**内存管理单元** （*MMU*）就做将虚拟内存地址转换成物理地址的工作。

而当进程访问的虚拟地址在页表中查不到时，系统会产生一个**缺页异常**，进入系统内核空间分配物理内存、更新进程页表，最后再返回用户空间，恢复进程的运行。

内存分页由于内存空间都是预先划分好的，也就不会像内存分段一样，在段与段之间会产生间隙非常小的内存，这正是分段会产生外部内存碎片的原因。而**采用了分页，页与页之间是紧密排列的，所以不会有外部碎片。**

但是，**因为内存分页机制分配内存的最小单位是一页**，即使程序不足一页大小，我们最少只能分配一个页，所以页内会出现内存浪费，所以针对**内存分页机制会有内部内存碎片**的现象。

如果内存空间不够，操作系统会把其他正在运行的进程中的「最近没被使用」的内存页面给释放掉，也就是暂时写在硬盘上，称为**换出**（*Swap Out*）。一旦需要的时候，再加载进来，称为**换入**（*Swap In*）。所以，一次性写入磁盘的也只有少数的一个页或者几个页，不会花太多时间，**内存交换的效率就相对比较高。**、

更进一步地，分页的方式使得我们在加载程序的时候，不再需要一次性都把程序加载到物理内存中。我们完全可以在进行虚拟内存和物理内存的页之间的映射之后，并不真的把页加载到物理内存里，而是**只有在程序运行中，需要用到对应虚拟内存页里面的指令和数据时，再加载到物理内存里面去。**

分页机制下，虚拟地址和物理地址是如何映射的？

在分页机制下，虚拟地址分为两部分，**页号**和**页内偏移**。页号作为页表的索引，**页表**包含物理页每页所在**物理内存的基地址**，这个基地址与页内偏移的组合就形成了物理内存地址，见下图。

<img src="https://cdn.xiaolincoding.com//mysql/other/7884f4d8db4949f7a5bb4bbd0f452609.png" style="zoom:80%;" />

总结一下，对于一个内存地址转换，其实就是这样三个步骤：

- 把虚拟内存地址，切分成页号和偏移量；
- 根据页号，从页表里面，查询对应的物理页号；
- 直接拿物理页号，加上前面的偏移量，就得到了物理内存地址。

#### 多级页表

我们把这个 100 多万个「页表项」的单级页表再分页，将页表（一级页表）分为 `1024` 个页表（二级页表），每个表（二级页表）中包含 `1024` 个「页表项」，形成**二级分页**。如下图所示：

<img src="https://cdn.xiaolincoding.com//mysql/other/19296e249b2240c29f9c52be70f611d5.png" style="zoom:80%;" />

#### TLB

多级页表虽然解决了空间上的问题，但是虚拟地址到物理地址的转换就多了几道转换的工序，这显然就降低了这俩地址转换的速度，也就是带来了时间上的开销。

程序是有局部性的，即在一段时间内，整个程序的执行仅限于程序中的某一部分。相应地，执行所访问的存储空间也局限于某个内存区域。

把最常访问的几个页表项存储到访问速度更快的硬件，于是计算机科学家们，就在 CPU 芯片中，加入了一个专门存放程序最常访问的页表项的 Cache，这个 Cache 就是 TLB（*Translation Lookaside Buffer*） ，通常称为页表缓存、转址旁路缓存、快表等。

### 段页式内存管理

 段页式内存管理实现的方式：

- 先将程序划分为多个有逻辑意义的段，也就是前面提到的分段机制；
- 接着再把每个段划分为多个页，也就是对分段划分出来的连续空间，再划分固定大小的页；

用于段页式地址变换的数据结构是每一个程序一张段表，每个段又建立一张页表，段表中的地址是页表的起始地址，而页表中的地址则为某页的物理页号，如图所示：

<img src="https://cdn.xiaolincoding.com//mysql/other/8904fb89ae0c49c4b0f2f7b5a0a7b099.png" style="zoom:80%;" />

段页式地址变换中要得到物理地址须经过三次内存访问：

- 第一次访问段表，得到页表起始地址；
- 第二次访问页表，得到物理页号；
- 第三次将物理页号与页内位移组合，得到物理地址。

可用软、硬件相结合的方法实现段页式地址变换，这样虽然增加了硬件成本和系统开销，但提高了内存的利用率。

# 第一章 计算机系统概述

## 1.1 操作系统基本概念

### 研梦错题

* 实时控制系统主要用于：**工业生产的过程控制、航天系统的跟踪和控制、武器制导等对响应速度要求非常高的系统**

  实时信息处理主要用于：**售票系统、信息查询和检索等对响应速度要求不是很高的系统中**
  
* 22年期末

  * 操作系统是计算机系统中的一个 **系统软件**，它管理和控制计算机系统中的 **资源**
  
* 17年真题

  * 操作系统的四大管理功能：**处理机管理、存储器管理、设备管理、文件管理**

### 王道错题

* 系统调用的目的是：**请求系统服务**

* 操作系统与用户通信结构通常不包括：**缓存管理指令**

  > 命令解释器属于命令接口；shell是命令解析器也属于命令接口；系统中的缓存全部由操作系统管理，操作系统不提供管理系统缓存的系统调用

* 不属于多道程序设计的基本特征的是：**顺序性**

  > 引入多道程序后，程序的执行就失去了封闭性和顺序性；程序执行因为共享资源及互相协同的原因产生竞争，相互制约，且程序的执行一般是断续的；顺序性是单道程序设计的基本特征

* 单处理机系统中，可并行的是：**处理机与设备、处理机和通道、设备和设备**

  > 通道是独立于CPU的IO设备，两者可以并行；



## 1.2 操作选题发展历程

### 研梦错题

1. 18年期末

   ![image-20240316150853490](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316150853490.png)

   > B **分时系统相比实时系统更强调交互性。**这是因为**分时系统设计为多用户的通用系统，它允许多个用户通过各自的终端与系统进行交互，用户可以几乎实时地得到系统的响应。分时系统的目标是优化用户体验，确保每个用户都感觉像是独占计算机资源**
   >
   > **实时系统则主要关注在规定的时间内响应外部事件**，它们通常是专用系统，用于控制或处理紧急任务，如工业控制系统、医疗监测系统等。**实时系统的交互性相对较弱，因为它们通常不是为了与用户进行频繁的交互而设计的，而是为了确保任务能够在严格的时间限制内完成。**   

2. 18年期末

   分布式操作系统：

   **是指把多个处理机通过线路互联而构成的系统，此系统的处理和控制分布在各个处理机上。**

   主要特点：分布性、自治性、模块性、并行性
   
3. 19年期末

   * 并行系统分为：**对称多处理** 和 **非对称多处理**
   
     > 对称多处理（SMP）和非对称多处理（AMP）是两种多处理器系统的配置方式。
     >
     > **对称多处理（SMP）**:
     >
     > - **所有处理器都是相同的，共享主存储器和I/O设备。**
     > - 每个处理器都可以执行任何任务，**操作系统视所有处理器为平等。**
     > - 任务可以在处理器之间分配，提高了系统的处理能力和效率。
     >
     > **非对称多处理（AMP）**:
     >
     > - **处理器之间存在主从关系，不是所有的处理器都相同**。
     > - **主处理器控制系统，分配任务给从处理器。**
     > - 通常用于特定任务需要并发执行的嵌入式系统，不需要SMP系统那样的通用计算能力。
     >
     > 简而言之，**SMP中所有处理器都可以独立处理任务，而AMP中有一个主处理器负责协调其他从处理器的工作。**    
   
4. 18年真题

   * 关于操作系统的论述中，正确的是：

     1. **对批处理作业，必须提供相应的作业控制信息**
     2. 对分时系统，不一定全部提供人机交互功能
     3. 从响应角度来看，分时系统与实时系统的要求相似
     4. 从交互角度看，分时系统和实时系统相似

     > 分时系统是要提供人机交互的功能的

### 王道错题

* 分时系统追求的目标是：比较快速的响应用户

  > 要求快速响应用户是导致分时系统出现的重要原因

* 相比单道程序系统，多道程序系统的优点：CPU利用率高、系统吞吐量大、IO设备利用率高

  > 至于系统开销，由于系统需要付出额外的开销来组织作业和切换作业，所以系统开销大
  
* 试述分布式操作系统有哪些功能，它与网络操作系统的区别是什么？

  > 1. 进程迁移、分布式进程同步、任务分配、资源管理
  > 2. 通信和资源方面，分布式OS的透明性强，系统中任意两个节点机之间无主从之分，都可以共享系统中的全部资源，多个节点机可以共同协作完成一个任务，可靠性高。若某一个节点机出现故障、系统任然可以正常工作，只是降价使用。而网络OS是共享服务器的资源，服务器是系统互联的瓶颈问题。



## 1.3 操作系统运行环境

### 研梦错题

1. 18年期末

   ![image-20240316150519893](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316150519893.png)

   > 程序中断通常指的是硬中断，可以理解为外部中断。
   >
   > 硬中断通常是由外部硬件设备引起的，如键盘输入或网络数据包到达，它们是异步的，可以在任何时候发生，通常需要立即处理以响应硬件事件。硬中断会直接打断CPU的当前执行任务，由中断控制器向CPU发出信号，然后CPU暂停当前进程，保存其状态，并执行中断服务程序。

### 王道错题

* 在通用操作系统管理下的计算机运行程序，不需要向操作系统预定运行时间

  > 通用操作系统使用时间片轮转调度算法，用户运行程序并不需要预先预定运行时间

* 在提供了核心态和用户态的操作系统下，IO指令必须在核心态下工作；且通道技术是一种硬件技术

  > IO指令需要中断操作，中断必须在核心态下执行；
  >
  > IO通道实际上是一种特殊的处理器，其具有执行IO指令的能力，并通过执行通道程序来控制IO操作

* 中断是操作系统必须提供的功能，因为计算机的各种错误都需要中断处理

  > 系统调用也是操作系统必须提供的功能，但是王道的题目可能有点奇怪
  
* 下列可以不需要硬件支持的是：进程调度

  > 1. 时钟管理：时钟管理中，需要硬件计数器保持时钟运行
  > 2. 地址映射：地址映射中，需要基地址寄存器和地址加法器的支持
  > 3. 中断系统：中断处理流程中，前三个步骤是由硬件直接实现的(隐指令)
  >
  > > 中断流程：
  > >
  > > 多重中断方式的中断服务程序包括：**保护现场、开中断、中断服务、恢复现场、中断返回**
  > >
  > > 完整的过程是
  > >
  > > **1.关中断，2.保存断点（PC、PSW），3.识别中断源，4.保存现场（通用寄存器），5.开中断，6.中断服务，7.关中断，8.恢复现场，9.开中断，10.中断返回**
  > >
  > > 其中，1-3由硬件完成，4-10由中断服务程序完成

* 中断发生后，进入中断处理的程序属于：操作系统程序

  > 即中断处理程序属于操作系统程序(内核程序)

* 在区分内核态和用户态的操作系统中，核心态->用户态**由操作系统执行应用程序进行转变的**；而用户态->核心态的转换则是**由硬件完成的**

  > 用户态到核心态是计算机通过中断机制完成的，但不是中断处理程序完成的；中断处理程序一般也在核心态执行，因此无法完成转换成核心态这一任务，核心态程序同理

* 只能在核心态下运行的指令是：置时钟指令、广义指令、输入输出

  > ![image-20240203204005428](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240203204005428.png)
  >
  > ![image-20240203204035830](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240203204035830.png)
  >
  > 13. 广义指令的调用是可以在用户态，但执行一定在核心态

* 不可能在用户态发生的事件是：进程切换

* ![image-20240316085714523](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316085714523.png)

  > B 子程序调用只需要保存程序断点，即下一条指令的地址；中断不仅要保存断点，还要保存PSW的内容。**中断中，最重要的就是PC和PSW**

* ![image-20240316085748788](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316085748788.png)

  > **B 外部中断处理过程中，PC由隐指令自动保存，通用寄存器由操作系统保存。**

* ![image-20240316085833736](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316085833736.png)

  > C 对于指令D，**可能会出现越界访问或者缺页异常**

* ![image-20240316090033824](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316090033824.png)

  > A **中断是指来自CPU执行以外的事件发生**，如设备发出的IO中断；时钟中断，表示一个固定的时间片已经完成；中断通常是与当前程序运行无关的事；
  >
  > **异常也称内中断，指的是源自CPU执行指令内部的事件**，如程序非法操作码、地址越界、算数溢出、虚存系统缺页、专门的陷入指令

* ![image-20240316090212398](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316090212398.png)

  > D 时钟中断的主要工作是处理和时间有关的信息及决定是否执行调度程序。和时间有关的所有信息包括：系统时间、进程的时间片、延时、使用CPU的时间、各种定时器。

* ![image-20240316090221380](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316090221380.png)

  > D 检测到中断后，**硬件自动保存程序断点（PC、PSW）**，然后硬件找到中断信号对应的中断向量、中断向量指明中断服务例行程序入口地址（**各中断向量统一存放在中断向量表，由OS初始化**），接下来**中断服务程序保存中断屏蔽字、各通用寄存器的值；中断服务程序属于操作系统内核。**

## 1.4、5、6 OS系统结构、OS引导、虚拟机、

### 研梦错题

1. 19年期末

   * 什么是系统调用？

     **系统调用是 在程序中能够用类似函数调用方式调用的操作系统服务集合。每一种操作系统服务占用一条系统调用命令(或函数)。系统调用是操作系统给用户提供的程序级接口。如果一个用户需要再程序中使用操作系统的服务，则只能通过系统调用；**

     **在操作系统中，系统调用程序在核心态执行，一般用户调用程序由用户准备，在用户态执行。**

### 王道错题

1. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094306083.png" alt="image-20240316094306083" style="zoom:80%;" />

   > C 单向依赖是分层式OS的特点。分层式OS中增加或替换一个层中的模块或整层时，只要不改变相应层之间的接口，就不会影响其他层，因而易于扩充和维护。
   >
   > **层次定义好之后，相当于各层之间的依赖关系也就固定了，因此往往不够灵活。**
   >
   > **每层执行一个功能，通常要自上而下的穿过多层，增加了额外的开销，导致系统效率降低。**

2. ![image-20240316094319705](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094319705.png)

   > C 微内核OS的主要缺点是性能问题，因为需要频繁地再核心态和用户态之间切换，因而切换开销增大。
   >
   > **宏内核的优缺点和微内核是对立的。**

3. ![image-20240316094330702](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094330702.png)

   > C 对于III，低级进程管理和调度属于调度功能的机制部分，应当将其放入内核。

4. ![image-20240316094351366](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094351366.png)

   > C 微内核的优点是：扩展性和灵活性、可靠性和安全性、可移植性、分布式计算
   >
   > 而缺点就是性能问题；

5. ![image-20240316094412168](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094412168.png)

   > C 对于IV，主要是最后一句使得系统更加高效是错误的；添加系统服务时不需要修改内核，是微内核架构的可扩展性和灵活性；

6. ![image-20240316094424746](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094424746.png)

   > C **CPU激活后，会从最顶端的地址FFFF0H获得第一条指令来执行，这个地址仅仅有16个字节，放不下一段程序，所以是一条JMP指令，跳到更低地址执行BIOS程序。BIOS程序在内存最开始的空间构建中断向量表和相应服务程序，在后续POST过程中要用到中断调用等功能。然后进行通电自检（POST-Power on self test）**。完成POST后BIOS要在硬盘等存储设备中搜寻操作系统内核的位置。

7. ![image-20240316094433010](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094433010.png)

   > A **BIOS将控制权交给排在首位的启动设备之后，CPU将该设备的主引导扇区的内容（主引导记录MBR）加载到内存中，然后由MBR检查分区表，查找活动分区，并将该分区的引导扇区的内容（分区引导记录PBR）加载到内存执行。**

8. ![image-20240316094442766](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316094442766.png)

   > A 对于III，整整并行的是多核处理机，多台虚拟机同时运行在同一物理机器上，相当于多道程序设计技术。
   >
   > 





# 第二章 进程与线程

## 概念

### 作业的基本概念

* 作业的组成

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222215115159.png" alt="image-20240222215115159" style="zoom:80%;" />

### 作业的输入输出方式

* **脱机输入输出**

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222215439158.png" alt="image-20240222215439158" style="zoom:80%;" />

* **联机输入输出**

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222215451407.png" alt="image-20240222215451407" style="zoom:80%;" />

* **SPOOLING非常重要** 

  即外围设备同时联机操作(Simultaneous Peripheral Operation On  Line) 。
  特点：兼具脱机和联机方式的优点，**可以实现联机方式下的主机和外围设备的同时工作，又称为假脱机。**
  技术基础：
  **多道程序设计技术**：一次可以容纳多个程序在主机上“同时”执 行。
  **通道技术**：<u>通道是一个独立于CPU的，专管输入/输出控制的处理 机，它控制外部设备与内存直接进行数据交换。</u>

  ![image-20240222215637202](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222215637202.png)

  * **原理：**

    ![image-20240222220033189](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222220033189.png)

### 作业注册

* **作业控制块(JCB, Job Control Block)**
  JCB是系统为管理作业设置的一个数据结构， **是系统中作业存在的唯一标志。**
  JCB里面记录有与作业相关的各种信息，**只有当作业退出系统时，JCB才被撤销。**

### 作业调度

* 作业执行过程

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222222411439.png" alt="image-20240222222411439" style="zoom:80%;" />

* 单道批处理系统作业调度

  * 单道批量处理系统特点：**一次只能将一个作业调入内存运行(没有并发)。**

  * **先来先服务调度算法(FCFS, First Come First Served)**

    优点：简单，公平
    缺点：周转时间长，吞吐量小，不利于对短作业的执行

  * **最短作业优先调度算法(SJF, Shortest Job First)**

    优点：降低作业平均周转时间， 提高系统吞吐量
    缺点：对长作业不利，出现  “饥饿”现象

  * **最高响应比优先调度算法(HRP, Highest Ratio Priority)**

    思想：为每个作业设置一个优先级，使之能够**尽量保证短作业优先执行，同时又不让长作业等待过长时间。**

    > **响应比计算方法：**
    > **RP ＝ 作业响应时间 / 作业运行时间**
    > **＝ (作业等待时间+ 作业运行时间) / 作业运行时间**
    > **＝ 1 + (作业等待时间 / 作业运行时间)**

    优点：既照顾到了短作业，也 考虑到了长作业，能够克服长 作业的“饥饿”现象，是一种 折中的算法。

    缺点：计算略微复杂

    > 例：
    >
    > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222222821222.png" alt="image-20240222222821222" style="zoom:80%;" />

* 多道批处理系统作业调度算法

  * 多道批处理系统的特点：**一次可以选择多个作业同时执行(并发)。**

  * 例：

    ![image-20240222222939607](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240222222939607.png)

    第二行的这种情况可以得到最佳的调度效率

* 作业调度算法性能分析
  评价因素

  * CPU利用率。希望能够获得较高的CPU利用率。显然**尽量让外设和CPU同时工作是提高CPU利用率的有效方法。**

  * 吞吐量：单位时间内CPU完成作业的数量。显然**短作业优先有利于提高吞吐量。**

  * 周转时间：**评价批处理系统的性能指标。**

    ![image-20240222223310596](../../../../../Typora/temppic/image-20240222223310596.png)

* 各类算法的示例（单道程序）

  ![image-20240222223409737](../../../../../Typora/temppic/image-20240222223409737.png)

  * FCFS：

    ![image-20240222223435109](../../../../../Typora/temppic/image-20240222223435109.png)

  * SJF

    ![image-20240222223449206](../../../../../Typora/temppic/image-20240222223449206.png)

  * 响应比高者优先

    ![image-20240222223513600](../../../../../Typora/temppic/image-20240222223513600.png)

  **就平均周转时间和平均周转系数来说，最短作业优先算法最小，先来先服务算法最大，响应比高者优先算法居中。**

* 多道程序环境下作业调度的性能

  ![image-20240222223650425](../../../../../Typora/temppic/image-20240222223650425.png)

  * 作业执行步骤
    * 进入内存：在此我们假设使用短作业优先调度算法。
    * 占有处理机：在此假设采用抢占式优先级调度算法(即一个作业正 在运行时可以暂停运行让出处理机给其它作业) ，规定：短作业享 有较高优先级。
  * ![image-20240222223724230](../../../../../Typora/temppic/image-20240222223724230.png)

### 接口

* 用户接口：

  作业(Job/Task)控制级接口：命令方式和图形界面。

* 程序接口：

  * 访管指令：在**算态**下执行的一条特殊的 指令**(不是特权指令)**。当源程序中有需要操作系统服务的要求时，**编译程序**就会在目标程序中安排一条“访管指令” 并设置一些参数。访管指令能引起访管中断，使得程序 “自愿进管” 。Unix  中又称为“陷阱指令”(trap）。

    > 访管指令功能：
    > (1)**能够使处理器从算态进入管态;  (2)操作执行完成后，从管态回到
    > 算态，使用户程序接着执行。**

  * 特权指令：操作系统中有一些CPU指令是非常危险的，如果错用，将导致整个系统崩溃，如**停机指令、置PSW指令、 中断操作指令（开、关、屏蔽中断）、 输入输出指令等**。**这类指令只能在管态 下运行而不能在算态下运行**

  ![image-20240222223846060](../../../../../Typora/temppic/image-20240222223846060.png)

* **系统功能调用与普通过程调用的区别**

  * 运行状态不同。

    系统功能调用的**调用过程和被调用过程运行在不同的状态**，而普通的过程调用**运行在相同的状态。**

  * 调用方法不同

    系统调用必须**通过中断机制首先进入系统核心**，**然后才能转向相应的命令处理程序**。普通过程调用可以**直接由调用过程转向被调用过程**。

  * 返回问题。

    普通的过程调用**直接返回调用过程继续执行**。对于系统功能调用，由于发生和状态的改变，在返回时**可能需要重新进行调度分析**(如 抢先式调度的系统)。

### 进程

#### 进程的引入

* 并发程序特征：程序执行的间断性、资源共享、独立和合作(制约)性；

  程序的并发执行使得程序的执行成为一个动态性很强的过程。而**程序是一个静态的概念**，不再能切实反映程序执行的各种特征。故而要引入新的概念来表示程序执行过程的这种新特性。

* 进程与程序的区别和联系

  * 区别

    1. 程序是**静态**的，是有序代码的集合；进 程是**动态**的，是程序的一次执行。
    2. 程序的永久的，没有生命周期，可长久 保存；进程是暂时的，有生命周期，是 一个动态不断变化的过程。
    3. 进程是操作系统资源分配和保护的基本 单位，程序没有此功能。
    4. 进程与程序的结构不同。

  * 联系

    通过多次执行，一个程序可对应多个进程；通过调用关系，一个进程可包括多个程序。

* 进程的组成

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223231706937.png" alt="image-20240223231706937" style="zoom:80%;" />

  1. PCB

     * 定义：是操作系统用来记录进程详细状态和相关信息的基本数据结构，它和进程是一一对应的，**是进程存在的唯一标识。**
       进程的档案，描述进程的特征，记载进程的历史，决定进程的命运。

     * 提供进程的各种信息，以便操作系统查询、控制和管理。

     * PCB结构

       <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223231836414.png" alt="image-20240223231836414" style="zoom:80%;" />

#### 进程的执行与控制

* 进程的基本状态及其转换

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223231950889.png" alt="image-20240223231950889" style="zoom:80%;" />

* 进程控制

  系统对进程的控制和管理是通过操作系统内核中的原语实现的

#### 进程调度

> 注意作业调度和进程调度的区别：作业调度是高级调度，从磁盘向内存调度；进程调度是低级调度，从内存向CPU调度

* **先来先服务调度算法 (FCFS, First Come First Served)**

  * 思想：先来先服务，保证公平性
  * 特点：简单可靠、实现方便、灵活性差；不能保证吞吐量、周转时间、及时性

* **基于优先数的调度算法(Priority Scheduling Algorithm)**

  * 思想：给每一个进程设置一个**优先数(优先级)**，系统在调度时优先选择具 有高优先级的进程占用CPU。具有相同优先数的进程按照FCFS算法执行。

  * 优先数的确定：**此处补充王道非抢占式优先级调度和抢占式优先级调度P65**
    * 静态优先数法：进程创建时就规定好它的优先数，这个数值在进程运行时不变。
    * 动态优先数法：进程的优先数在执行过程中可以根据情况变化而改变。

* **时间片轮转法(RR, Round Robin)**

  * 思想：系统规定一个**时间长度(时间片/时间量)**作为允许一个进程运行 的时间，如果在这段时间该进程没有执行完，则必须让出CPU等待下一次分配的时间片。
  * 特点：专门为分时系统设计，较为公平性，对用户反应及时。不保证公平性、吞吐量、周转时间；保证及时性

* **多级反馈队列调度算法(Multilevel Feedback Queue  Scheduling)** **此算法建议看王道，非常重要**

  * 思想：引入多个就绪队列，通过对各队列的区别对待，达到一个综合的调度目标。

  * 原理：

    ![image-20240223233324765](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223233324765.png)

#### 进程间的相互作用

* 同步

  进程之间这种相互合作、协同工作 的关系称为进程的同步。

  简单说来就是：多个相关进程在执行次序上的协调。

  制约关系：直接制约。

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223233423360.png" alt="image-20240223233423360" style="zoom:80%;" />

* 互斥

  > 临界资源：也称独占资源，是指在一段时间内只允许一个进程访 问的资源。例如打印机，磁带机，也可以是进程共享的数据、变量等。

  多个进程因为争夺**临界资源**而相互排斥执行的过程称为进程的互斥。
  制约关系：间接制约。

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240223233552128.png" alt="image-20240223233552128" style="zoom:80%;" />
  
* 进程并发会出现不确定性，是的变量访问出现问题。

* **锁变量法(测试和设置指令)**

  设置一个共享变量W(锁)，初值为0。当一个进程想进入其临界区(进程中涉及临界资源的程序段)时，它首先测试这把锁：如果锁的值为0，则进程将其置为1并进入临界区。若锁已经为1，则进程等待直到其变成0。

  ![image-20240226114916882](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226114916882.png)

* **信号量(Semaphore)和P、V操作**

  * 特点：
    表示资源的实体——是一个与队列有关的整型变量。
    其值只能通过初始化操作和
    P、V操作来访问

  * 信号量的类型：
    公用信号量：用于进程间的互斥，初值通常为1；
    私有信号量：用于进程间的同步，初值通常为0或n。

  * P操作：意味着请求分配一个单位资源。V操作：意味着释放一个单位资源。

    **V操作在唤醒别人的同时不会阻塞调用进程。**

    **P、V操作（对同一信号量）总是成对出现的；互斥操作时他们处于 同一进程中；同步操作时他们处于不同进程中。
    P、V操作的位置和次序十分重要，使用时注意逻辑关系。**

  * P、V操作实现互斥操作示例：

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226115342744.png" alt="image-20240226115342744" style="zoom:80%;" />

  * P、V操作实现进程同步操作示例：

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226115443376.png" alt="image-20240226115443376" style="zoom:80%;" />

* P、V操作经典问题

  * 读者写者问题：

    问题描述：一个数据对象（文件、记录）可以为多个并发进程共享。 其中有的进程只需要读其中的内容，我们称为“读者”；有的进程负 责更新其中内容，我们称为“写者”。规定：“读者”可以同时读取 共享数据对象；“写者”不能和其它任何进程同时访问共享数据对象。

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226115620726.png" alt="image-20240226115620726" style="zoom:80%;" />

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226115712600.png" alt="image-20240226115712600" style="zoom:80%;" />

  * 理发师问题

    问题描述：理发店有一位理发师，一把理发椅和n把用来等候理发的椅子。 如果没有顾客，则理发师在理发椅上睡觉，顾客理来时，如理发师闲则理发， 否则如有空椅则坐等，没有空椅则离开，编写程序实现理发师和顾客程序

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226115752377.png" alt="image-20240226115752377" style="zoom:80%;" />

  * 哲学家问题（讲课划掉了）

    五个哲学家围坐在一圆桌旁，桌中央有一盘通心粉，每人面前有一只 空盘子，每两人之间放一只叉子。每个哲学家的行为是思考，感到饥 饿，然后吃通心粉。为了吃通心粉，每个哲学家必须拿到两只叉子， 并且每个人只能直接从自己的左边或右边去取叉子。

* **管程**

  管程是由**一组局部的变量、对局部变量进行操作的一组过程**以及**对局部变量进行初始化的语句序列**构成的一个软件模块

  * 思想：集中和封装针对一个共享资源的所有访问，包括所需的同步操作

  * 结构：

    ![image-20240226120434362](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240226120434362.png)

  * 特点：

    1. **局部于管程的数据只能被局部于管程内的函数所访问**。
    2. 一个进程**只有通过调用管程内的函数才能进入管程访问共享数据**。
    3. 每次**仅允许一个进程在管程内执行某个函数**。
    4. 由于管程是一个语言成分，所以**管程的互斥访问完全由编译程序在编译时自动添加上**
    5. 无需程序员关心，而且保证正确。

## 2.1 进程与线程

### 研梦错题

1. 18年期末

   Unix将进程控制块分为 **PROC和USER** 两部分。

2. 18年期末

   **多道程序设计是利用了CPU和通道的并行性来提高系统的效率**

3. 18年期末

   线程与进程的区别：

   * 地址空间和资源不同：进程间相互独立，同一进程的各个线程之间却互相共享
   * 通信不同：进程间使用IPC通信，线程间可以直接读写进程数据段来通信，但是需要进程同步或互斥手段辅助
   * 调度和切换不同：线程上下文切换比进程上下文切换快得多。
   
4. 17年真题

   * 进程的结构、特征、基本状态
     * 结构：**PCB、程序段、相关数据**
     * 特征：**动态性、独立性、异步性、并发性**
     * 基本状态：**就绪态、运行态、阻塞态**

### 王道错题

1. ![image-20240311120533922](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311120533922.png)

   > C 

2. ![image-20240311120711954](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311120711954.png)

   > C 进程各自的地址空间是私有的，只能执行自己地址空间中的程序，且只能访问自己地址空间中的数据，互相访问会导致指针的越界错误。**但是可以利用操作系统提供的：共享文件、消息传递、共享存储区等通信**

3. ![image-20240311120724341](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311120724341.png)

   > A 动态性是进程最重要的特性

4. ![image-20240311120929083](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311120929083.png)

   > **A 对于C，系统发生死锁时，有可能全部进程都处于阻塞态，CPU空闲**

5. ![image-20240311121000339](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121000339.png)

   > D 程序封闭性是指进程的执行结果只取决于进程本身，不受外界影响。失去封闭性后，**不同速度下的执行结果不同**

6. ![image-20240311121540936](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121540936.png)

   > C 对于A，假如有一个内核进程，它映射到用户级后有多个线程，那么这些线程之间切换是不需要在内核级切换进程，也就不需要内核的支持。
   >
   > 对于B、D，**引入线程后， 进程仍然是资源分配的单位。内核级线程是处理器调度和分派的单位，线程本身不具有资源，它可以共享所属进程的资源。**

7. ![image-20240311121550567](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121550567.png)

   > B **在多对一的线程模型中，由于只有一个内核级线程，用户级的多线程对操作系统透明，因此操作系统内核只能感知到一个调度单位的存在。因此该进程的一个线程被阻塞后，该进程就被阻塞，进程的其他线程也被阻塞。**

8. ![image-20240311121603353](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121603353.png)

   > A 仅仅说进程是在多程序环境下的完整程序是不合适的，它还包括PCB、程序和数据以及执行栈区。而程序是静态的，进程是动态的。

9. ![image-20240311121644826](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121644826.png)

   > B、D、D、C、B、A
   >
   > C语言编写的程序在使用内存时分为三个段，
   >
   > 正文段：代码、赋值数据段、常量
   >
   > 数据堆段：动态分配的存储区
   >
   > 数据栈段：临时变量
   >
   > **由此推断，全局赋值变量在正文段的赋值数据段，未赋值的局部变量和实参传递在栈段，常量在正文段，进程优先级只能在PCB中**

10. ![image-20240311121654206](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121654206.png)

    > B 程序代码经过多次创建可对应不同进程，**而同一个系统的进程可以由系统调用的方法被不同进程多次使用。**

11. ![image-20240311121703106](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121703106.png)

    > **D PCB内容主要有四大类：进程标志信息、进程控制信息、进程资源信息、CPU现场信息**

12. ![image-20240311121718302](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311121718302.png)

    > C 进程就绪的数目越多，争夺CPU的进程就越多，**但只要就绪队列不为空，CPU就总是可以调度进程运行，保持繁忙。**

13. ![image-20240311163226275](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311163226275.png)

    > B **对进程的管理和控制功能是通过执行各种原语实现的**

14. ![image-20240311163249569](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311163249569.png)

    > C 整个系统只有一个键盘，而且速度较慢，完全可以使用一个线程来处理整个系统的键盘输入

15. ![image-20240311163300964](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311163300964.png)

    > C 不同的进程拥有不同的代码段和数据段，全局变量是对同一进程而言的，在不同的进程中不同的变量，没有联系，所以不能用于数据交换。
    >
    > 我的想法：注意区分进程和线程的共享方式，**此处建议复习进程共享数据的方式，P40**
    >
    > **Bing：同一进程中的线程可以通过全局变量、进程地址空间实现数据交换**

16. ![image-20240311164138143](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164138143.png)

    > B

17. ![image-20240311164151617](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164151617.png)

    > **A 进程之间通信方式主要有管道、消息传递、共享内存、文件映射、套接字等**

18. ![image-20240311164203865](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164203865.png)

    > B 采用轮转式调度算法，如果进程中分别设置100个用户级线程和100个内核级线程，则前者的执行时间是后者的100倍 ？ 
    >
    > 解释：用户级线程（User-Level Threads）完全由用户进程管理，操作系统内核不直接参与其调度。在轮转式调度算法中，操作系统分配给进程的时间片需要在该进程的所有用户级线程之间共享。**因此，如果一个进程有100个用户级线程，每个线程实际上只能获得1/100的时间片，因为它们必须在单个进程的时间片内轮转。**
    >
    > 另一方面，内核级线程（Kernel-Level Threads）由操作系统内核直接管理和调度。每个内核级线程可以被视为独立的调度单位，能够获得完整的时间片。**如果一个进程有100个内核级线程，在轮转式调度算法下，每个线程都能获得一个完整的时间片，从而实现并行执行。**
    >
    > **所以说，前者进程运行完毕需要的时间是后者进程运行完毕需要的时间的100倍**
    >
    > 对于C、D，用户级线程不需要内核的支持，与系统平台无关，对线程管理的代码属于用户程序的一部分。

19. ![image-20240311164221134](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164221134.png)

    > A **在内核级线程中，同一进程的线程切换，需要从用户态切换到核心态进行，系统开销较大。**
    >
    > **CPU调度是在内核进行的，在内核级线程中，调度是在线程一级进行的，因此内核可以同时调度同一进程的多个线程在多CPU上并行运行，用户级线程则不行**
    >
    > 当进程中的内核级线程运行在内核态时，说明该进程也运行在内核态
    >
    > 我的想法：**此处建议复习线程实现方式，P43**

20. ![image-20240311164236321](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164236321.png)

    > C I，**用户登陆成功后，系统要为此创建一个用户管理的进程**，包括用户桌面、环境，所有用户进程都会在该进程下创建、管理
    >
    > **II，设备分配是通过在系统中设置相应的数据结构实现的，不需要创建进程**

21. ![image-20240311164306632](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311164306632.png)

    > A 选项D中进程优先级不应该在此时降低，应该在时间片用完后降低

22. ![image-20240311170843520](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311170843520.png)

    > C **管道是一种固定大小的缓冲区，管道的大小通常为内存的一页，其大小并不受磁盘容量大小限制**
    >
    > **一个普通管道只允许单向通信，若要实现父子进程双向通信，则需要定义两个管道，每个管道向不同方向发送数据。**
    >
    > 当管道满时，写进程在写管道被阻塞，当管道空时，读进程在读管道被阻塞；**不可以一边写一边读**
    >
    > 父进程创建一个管道后，由于管道是一种特殊文件，**因此所有的子进程也继承了父进程管道，并使用它与父进程通信**

23. ![image-20240311170854603](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311170854603.png)

    > **B 对于B，在多线程模型中，用户级线程和内核级线程的连接方式有多对一、一对一、多对多，操作系统为每个用户级线程建立一个线程控制块属于一对一模型**
    >
    > **用户级线程的管理工作可以只在用户空间中进行，因此可以在不支持内核级线程的操作系统上实现**

24. ![image-20240311170907433](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311170907433.png)

    > B **父进程和子进程共享一部分资源，但不能共享虚拟地址空间**，在创建子进程时，会为子进程分配资源，如虚拟地址空间

## 2.2 处理机调度

### 研梦错题

1. *17年期末

   ![image-20240314112714574](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314112714574.png)
   
   > 不可抢占式静态优先级调度会使得优先级低的进程饿死，长期得不到运行。
   
2. *17年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314121029592.png" alt="image-20240314121029592" style="zoom:80%;" />

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314121045073.png" alt="image-20240314121045073" style="zoom:80%;" />

   > **注意，本题非想象的只要内存空间够就先将高响应比的作业调入内存**
   >
   > 首先，对于J1，J1先到，所以最先执行，结束时间10.48，周转时间42min
   >
   > 当J1结束时，各个作业的响应比是
   >
   > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316121105859.png" alt="image-20240316121105859" style="zoom:50%;" />
   >
   > 由高响应比优先调度算法，选择J2，其开始时间是10.48，结束时间是11.18，周转时间是59min
   >
   > 在J2结束后，响应比为
   >
   > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316121232745.png" alt="image-20240316121232745" style="zoom:50%;" />
   >
   > 选择J5，其开始时间是11.18，结束时间是11.30，周转时间48min；J5结束后，响应比为
   >
   > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316121327529.png" alt="image-20240316121327529" style="zoom:50%;" />
   >
   > 选择J3，其开始时间是11.30，结束时间是11.54，周转时间84min；J3结束后，只能选J4
   >
   > 其开始时间是11.54，结束时间是12.18，周转时间是102min
   >
   > 所以执行顺序是J1->J2->J5->J3->J4
   >
   > 平均周转时间是（42+59+84+102+48）/5=67min
   
3. 19年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240318111138400.png" alt="image-20240318111138400" style="zoom: 80%;" />

   > 在操作系统(OS)中，**短期计划（也称为进程调度或CPU调度）是指从就绪队列中选择一个进程，并将其分配给处理器执行的过程**。这个过程的目标是提高系统性能，确保CPU资源得到有效利用。短期调度的频率很高，通常在每个时间片结束时执行，它决定了哪个就绪状态的进程将获得CPU资源。
   >
   > **补充：低级调度、中级调度、高级调度**；即**短期调度、中期调度、长期调度**
   >
   > **低级调度：即进程调度，线程调度也属于低级调度**，决定将处理机分配给就绪进程队列的哪个进程
   >
   > **中级调度：即内存调度，一个进程会多次被调入或调出，中级调度频率比高级调度频率高；目的是提高内存利用率和系统吞吐量**；负责将内存中暂时不具备运行条件的进程交换到外存交换区，但内存空闲时，又将外存中具备运行条件的进程重新重新换入内存
   >
   > **高级调度：即作业调度，按照调度算法从外存上的作业队列中选择一个运行**；决定把外存中处于后备队列中的哪些作业调入内存，并为他们创建进程，分配资源，然后插入就绪队列    
   
4. 18年真题

   * 作业调度性能的优劣主要用 **平均周转时间、带权周转时间** 衡量

### 王道错题

1. ![image-20240311204950630](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311204950630.png)

   > B **FCFS有利于长作业，不利于短作业；CPU繁忙型作业要占用很长的CPU时间，相当于长作业；而IO繁忙型可能频繁放弃CPU，相当于短作业，所以SJF比较合适。**

2. ![image-20240311205315360](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311205315360.png)

   > **D 在同一台处理机上以单道方式运行时，要想获得最短的平均周转时间，使用SJF有较好的效果**

3. ![image-20240311205328084](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311205328084.png)

   > D 优先级算法中，IO繁忙型优于计算繁忙型；系统进程优先级高于用户进程；
   >
   > 我的想法:那是交互型进程由于非交互型进程，不是用户进程

4. ![image-20240311205356448](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311205356448.png)

   > B **作业是从用户角度出发的，由用户提交，以用户为单位；**进程是从操作系统出发的，由系统生成，是操作系统的资源分配和独立运行基本单位；

5. ![image-20240311210001028](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311210001028.png)

   > A 对于I，**分时系统中，响应时间与时间片和用户数成正比。**
   >
   > 对于III，中断向量本身是用于存放中断服务例行程序的入口地址，因此中断向量地址应该是入口地址的地址

6. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311210033999.png" alt="image-20240311210033999" style="zoom:67%;" />

   > C 对于C，当程序处于临界区时，只要不破坏临界资源的使用规则，就不会影响处理机的调度。
   >
   > 我的想法：那么B的错误说法是不是”创建新进程后 **会** 进行处理机调度“ 呢？

7. ![image-20240311210509653](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311210509653.png)

   > **B 对于优先级调度算法，IO型作业优先级高于计算型作业，所以要选择IO繁忙型作业有更高的优先级**

8. ![image-20240311210520376](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311210520376.png)

   > B 对于A，**进程切换带来系统开销，切换次数越多，开销越大；**

9. ![image-20240311211027638](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311211027638.png)

   > C 
   >
   > 我的想法：**除了正常调度，0时刻还有一个调度哦**

## 2.3 同步与互斥

### 研梦错题

1. 17年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314111032111.png" alt="image-20240314111032111" style="zoom:80%;" />

   > 对于A Bing：管程（Monitor）确实是一种同步机制，用于控制对共享资源的访问，确保在任何时刻只有一个进程可以使用该资源。但是，**管程本身并不是用来保证进程间的互斥（mutual exclusion），而是通过它提供的条件变量（condition variables）来实现进程间的同步。**条件变量可以使进程在某些条件不满足时阻塞，并在条件满足时被唤醒。所以，这句话中的错误在于管程并不直接保证进程互斥地访问共享变量，而是提供了一种机制来实现这一点。
   >
   > 对于D中的并发，王道P93，**管程定义了一个数据结构和能为并发进程所执行（在该数据结构上）的一组操作。**
   
2. 17年期末

   ![image-20240314202320021](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314202320021.png)

   ![image-20240314202334881](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314202334881.png)

   > 答案：
   >
   > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314202419965.png" alt="image-20240314202419965" style="zoom:67%;" />
   >
   > <img src="../../../../../../Typora/temppic/image-20240314202433959.png" alt="image-20240314202433959" style="zoom:67%;" />
   >
   > 我的答案中，第二个copy函数并没有实现对两个缓冲区的互斥操作，以为是直接从buffer1直接copy到buffer2，这里需要注意
   
3. 18年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316114005224.png" alt="image-20240316114005224" style="zoom:80%;" />

   > 王道P108第10题第二问
   >
   > 桥上可以同方向多车行驶，需要设置bridge，还需要对同方向车辆计数。为了防止同方向计数中同时申请bridge造成同方向不可同时行车的问题，要对计数过程加以保护，因此设置信号量mutexSN、mutexNS
   >
   > ~~~C
   > int countSN = 0;	//表示从南到北的汽车数量
   > int countNS = 0;	//表示从北到南的汽车数量
   > semaphore mutexSN = 1;	//用于保护countSN
   > semaphore mutexNS = 1;	//用于保护countNS
   > semaphore bridge = 1;	//用于互斥地访问桥
   > 
   > StoN() {
   >     P(mutexSN);
   >     if(countSN == 0)
   >     	P(bridge);
   >     countSN++;
   >     V(mutexSN);
   >     过桥;
   >     P(mutexSN);
   >     countSN--;
   >     if(countSN == 0)
   >         V(bridge);
   >     V(mutexSN);
   > }
   > 
   > NtoS() {
   >     P(mutexNS);
   >     if(countNS == 0)
   >         P(bridge);
   >     countNS++;
   >     V(mutexNS);
   >     过桥;
   >     P(mutexNS);
   >     countNS--;
   >     if(countNS == 0)
   >         V(bridge);
   >     V(mutexNS);
   > }
   > ~~~
   >
   
4. 19年期末

   ![image-20240318114030208](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240318114030208.png)

   ![image-20240318114044206](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240318114044206.png)

   ![image-20240318114104199](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240318114104199.png)
   
   > 1. buffer != 0
   > 2. pthread_cond_signal(&condc);
   > 3. pthread_mutex_unlock(&the_mutex);
   > 4. buffer == 0
   > 5. &condc
   > 6. pthread_mutex_unlock(&the_mutex);
   >
   > 似乎在不完全看懂的情况下也能蒙出来大半
   
5. 18年真题

   * 并发程序之间的相互制约，是由于它们 **共享资源、互相合作** 而产生的，因而导致程序在并发执行时，具有间断性特征
   * 为了是心啊对临界区的共享，应保证进程互斥地进入自己的临界区，为此在每个进程中的临界区前面应该设置 **wait操作**，在临界区之后设置 **signal操作**

### 王道错题

1. ![image-20240311211247013](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311211247013.png)

   > C 进程进入临界区必须满足互斥条件，当进程进入临界区但尚未离开时就被迫阻塞是可以的；在此状态下，只要其他进程在运行过程中不进入该进程的临界区就允许其运行。

2. ![image-20240311211312061](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311211312061.png)

   > D 同步机制的四个准则是：空闲让进、忙则等待、让权等待、有限等待
   >
   > **此处建议复习四个准则的含义、死锁的四个条件和含义，P86、134**

3. ![image-20240311211321978](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311211321978.png)

   > B **临界资源和共享资源的区别在于，一段时间内能否允许多个进程访问，显然自盘属于共享设备。公用队列可供多个进程使用，但一次只可供一个进程使用。**
   >
   > 私有数据仅供一个进程使用，可重入代码一次可供多个进程使用。

4. ![image-20240311212029998](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311212029998.png)

   > C **管程的signal操作和信号量机制中的V操作不同，信号量机制中的V操作一定会改变信号量的值S=S+1。而管程中的signal操作是针对某个条件变量的，若不存在因该条件而阻塞的进程，则signal不会产生任何影响。**

5. ![image-20240311212538006](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311212538006.png)

   > C:系统中有N个进程，其中至少有个一进程在执行，因此就绪队列中进程数量最多有n-1个
   >
   > B：死锁就是n个进程都被阻塞

6. ![image-20240311212109800](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311212109800.png)

   > C 每个进程中访问临界资源的那段代码称为临界区。
   >
   > I错误，银行家算法是避免死锁算法；
   >
   > **此处建议弄清哪些是避免算法，哪些是预防算法，P135，136**
   
7. ![image-20240311213132474](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311213132474.png)

   > B **信箱通信是一种间接通信方式**
   >
   > 我的想法：这题我也不知道哪来的，，，

8. ![image-20240311213153884](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311213153884.png)

   > D 是Peterson算法的实现。**P87，可以去看看王道的课理解**
   >
   > 想象为两个人进门，每个人进门前都会客套一句“你先走”。若进门时没别人，就当和空气说废话，然后进门；如果两个人同时进门，就互相先请，但各自只客套一次，所以先客套的人清完对方就等对方请自己，然后进门

9. ![image-20240311213225527](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311213225527.png)

   > B **当进程退出临界区时会置lock为false，负责唤醒处于就绪态的进程。**
   >
   > **等待进入临界区的进程会一直停留在执行while(TSL(&lock))的循环中，不会主动放弃CPU**
   >
   > **通过上述分析，发现并不满足让权等待原则**
   >
   > while(TSL(&lock))在关中断下执行，如果TSL(&lock)一直为true，那么系统可能会因此终止

10. ![image-20240311214216905](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311214216905.png)

    > D **若进程A执行了x.wait操作，该进程会阻塞，并挂到条件变量x的阻塞队列上。这样管程的使用权被释放，就可以有另一个进程进入管程；若进程B执行了x.signal，则会唤醒x对应阻塞队列上的队首进程。**

11. ![image-20240311214226077](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311214226077.png)

    > C 硬件方式实现进程同步时不能实现让权等待，B、D错误；
    >
    > Peterson算法实现了有限等待但不满足让权等待，A错误
    >
    > **记录型信号量引入阻塞机制，消除了不让权等待的情况**

12. ![image-20240311214251415](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311214251415.png)

    > C IV是让权等待，不一定非得实现，如Peterson算法

## 2.4 死锁

### 研梦错题

* 当系统中采用<u>资源有序分配方法</u>来预防死锁时，破坏了死锁的四个必要条件中的 **环路条件（循环等待）**，采用 **静态资源分配**方法预防死锁时可以破坏死锁的条件中的<u>保持和等待条件</u>

* 17年期末

  ![image-20240314214459118](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314214459118.png)
  
  > 主要看第二问，是一种没见过的考法
  >
  > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314214552278.png" alt="image-20240314214552278" style="zoom:80%;" />
  >
  > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314214604316.png" alt="image-20240314214604316" style="zoom:80%;" />
  
* 18年期末

  <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316113212720.png" alt="image-20240316113212720" style="zoom:80%;" />

  > D 粗心，只要无法满足至少一个进程可以获得全部资源的，那么都可能发生死锁；
  >
  > 本题中，除了D，其他几个都可以满足这个条件。

* 22年期末

  * 什么是死锁？产生死锁的原因和必要条件是什么？

    > 1. 在多道程序系统中，当一组进程中的每个进程均无限期地等待被改组进程中的另一进程所占有且永远不会释放的资源，此时的系统处于死锁状态，简称死锁
    > 2. 死锁原因：系统提供的资源有限、进程推进顺序不当
    > 3. 必要条件：互斥访问、不可抢占、请求和保持、循环等待

  * <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240320110953547.png" alt="image-20240320110953547" style="zoom: 67%;" />

    <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240320111008747.png" alt="image-20240320111008747" style="zoom: 80%;" />

    指出它们并发执行时，可能发生的与时间有关的错误

    > 进程B执行了print(N)后被中断，在执行N=0之前插入了进程A执行N=N+1；
    >
    > 则出现与时间有关的错误

### 王道错题

1. ![image-20240311215253958](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215253958.png)

   > **D 死锁避免是指在资源动态分配过程中用某些算法加以限制，防止系统进入不安全状态从而避免死锁发生。** B是避免死锁的后果，不是措施的原理

2. ![image-20240311215304514](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215304514.png)

   > **D A判断是否为不安全状态，B破坏了占有请求条件，C破坏了非剥夺条件**

3. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215324765.png" alt="image-20240311215324765" style="zoom:80%;" />

   > **B 银行家算法为死锁避免算法，死锁检测算法和资源分配图化简法为死锁检测，有序分配算法为死锁预防策略。**

4. ![image-20240311215741991](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215741991.png)

   > **D II，这是死锁产生的两大原因**

5. ![image-20240311215754324](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215754324.png)

   > ![image-20240311221850702](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311221850702.png)
   >
   > ![image-20240311221900876](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311221900876.png)

6. ![image-20240311215814942](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215814942.png)

   > 这种题目还是要好好分析
   >
   > ![image-20240311221925680](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311221925680.png)

7. ![image-20240311215825754](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311215825754.png)

   > C 死锁定理是用于检测死锁的方法。
   >
   > **此处建议复习死锁定理，P140**

8. ![image-20240311220117555](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311220117555.png)

   > B 对于A，银行家算法是死锁避免算法
   >
   > **对于B，根据定义可以这样说**

9. ![image-20240311220127195](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311220127195.png)

   > **B 对于I，限制用户申请资源的顺序，指的是死锁预防中的顺序资源分配法**

10. ![image-20240311220139195](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311220139195.png)

    > C 对于本题，先满足一个进程的资源需求，再看其他进程是否出现死锁状态。
    >
    > ![image-20240311221226034](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311221226034.png)

11. ![image-20240311220153819](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311220153819.png)

    > B **死锁预防是死锁处理策略(死锁预防、死锁避免、死锁检测，三种方法严苛性递减)中最为严苛的一中策略，破坏死锁产生的四个必要条件之一，可以确保系统不发生死锁。**
    >
    > 银行家算法用计算动态资源分配的安全性以避免系统进入死锁状态，**不能用于判断系统是否处于死锁**

# 第三章 内存管理

## 3.1 内存管理概念

### 研梦错题

1. 18年期末

   把作业装入内存时完成地址变换的方式称为 **静态重定位** ，而在作业执行期间（访问到数据或指令）才进行地址变换的方式称为 **动态重定位**。
   
2. 17年真题

   * 采用动态重定位方式装入的作业，在执行中允许 **操作系统有条件的** 将其移动

### 王道错题

1. ![image-20240311095935144](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311095935144.png)

   > C 内存保护是内存管理的一部分，是操作系统的任务，但是出于安全性和效率考虑，必须由硬件实现（比如界地址寄存器、重定位寄存器）

2. ![image-20240311095946558](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311095946558.png)

   > A 覆盖技术是早期在单一连续存储管理中使用的，**同样可用于固定分区分配**
   >
   > **此处建议复习非页式、段式分配方式和装入方式，P160、164**

3. ![image-20240311100007927](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311100007927.png)

   > D 静态装入是指在编程阶段就把物理地址计算好
   >
   > 可重定位装入：在装入时把逻辑地址转换成物理地址，但装入后不能改变
   >
   > 动态重定位：**在执行时再决定装入的地址并装入，装入后有可能换出，所以同一模块在内存中的物理地址可能改变；通过硬件地址转换机制实现**

4. ![image-20240311100025304](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311100025304.png)

   > A 固定分区方式中，作业装入后位置不再改变，可以采用静态重定位；

5. ![image-20240311100034331](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311100034331.png)

   > A 可变分区管理中，回收空闲区时采用拼接技术对空闲区进行合并。
   >
   > 我的想法：原来拼接技术不是将内存整合，将零散分布的分配内存合并到一起；那种技术叫做 **内存碎片整理或内存压缩**

6. ![image-20240311101732408](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311101732408.png)

   > B 多进程执行通过内存保护实现互不干扰，如页式管理中的页地址越界保护，段式管理中有段地址越界保护

7. ![image-20240311101753934](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311101753934.png)

   > C 页表和段表都在内存中，系统提供给用户的物理地址空间总大小为总空间大小减去页表或段表大小；但是页表和段表大小不能确定

8. ![image-20240311101805400](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311101805400.png)

   > B 内存分页管理是在硬件和操作系统层面实现的，对用户、编译系统、链接装配程序等上层不可见

9. ![image-20240311101822903](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311101822903.png)

   > A 因为系统处理器在同一时刻只能执行一条指令或访问数据，所以为每道程序设计一个寄存器没必要也不现实，只需要在却换程序执行时重置寄存器内容。

10. ![image-20240311102423636](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311102423636.png)

    > A 程序的动态链接与程序的逻辑结构相关，分段存储管理将程序按照逻辑段进行划分，因此有利于动态链接

11. ![image-20240311102434057](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311102434057.png)

    > D **可重入程序主要是通过共享来使用同一块存储空间的**，或通过动态链接的方式将所需程序段映射到相关进程中，**最大的优点就是减少了对程序段的调入调出，减少了对换数量**

12. ![image-20240311102453923](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311102453923.png)

    > A 动态分区，**在系统启动后，除操作系统占据一部分内存，其余空间是空闲区**；若作业申请内存，则从空闲区中划出一个与作业需求量相适应的分区分配；

13. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311102518481.png" alt="image-20240311102518481" style="zoom:80%;" />

    > D **覆盖和交换技术本质上是一种时间换空间的技术**，如果时间代价大到一定程度，那么覆盖和交换技术就没有意义了，所以应该以提高换入换出速度为管理目标

14. ![image-20240311103403163](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311103403163.png)

    > B 确定页面大小有很多因素，如**进程平均大小，页表占用长度**。而一旦确定，**所有页面就是等长的。**

15. ![image-20240311103440192](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311103440192.png)

    > B 这里指主存的访问，不是分配。**对主存的访问是以字节或字为单位的**

16. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311103733021.png" alt="image-20240311103733021" style="zoom:67%;" />

    > A **在多个进程并发执行时，所有进程的页表大多数驻留在内存中**，在系统中只设置一个页表寄存器，其存放页表在内存中的始址和长度。进程未执行时，页表始址和长度存放在PCB中，当调度到进程后，才将这两个数据装入页表寄存器。**每个进程有一个单独的逻辑地址，有一张属于自己的页表。**

17. ![image-20240311103752969](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311103752969.png)

    > C 对于IV，**静态重定位是在程序运行之前由装配程序完成的，必须分配全部连续的内存空间。**

18. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311105104377.png" alt="image-20240311105104377" style="zoom:67%;" />

    > C 在编译时，形成了针对每个模块的从0开始的逻辑地址，而在链接时才形成了整个程序的完整的从0开始的逻辑地址

19. ![image-20240311105117200](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311105117200.png)

    > B 段的共享是**通过两个作业的段表中相应表项指向被共享的同一个物理副本来实现的，因此在内存中仅保存一份段S的内容。**
    >
    > 段S对于进程P1、P2来说，使用位置可能不同，**所以在不同进程中的逻辑段号可能不同**；
    >
    > **段表项存放的是段的物理地址，对于共享段S来说，物理地址唯一，C正确。**

20. ![image-20240311105128421](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311105128421.png)

    > B 多级页表中，页表基址寄存器放的是顶级页表的起始物理地址，也就是一级页表。
    >
    > 顶级是一级，次级是二级。

## 3.2 虚拟内存管理

### 研梦错题

1. 19年期末

   * 什么是TLB？

     TLB是一个小型硬件设备将虚拟地址直接映射到物理地址，不必再访问列表。通常存在MMU中，包含少量的表项，每个表项记录了一个页面的相关信息，包括虚页号、页面修改位、保护码(R/W/E权限)和该页所对应的物理页框。是一个内存管理单元用于改进虚拟地址到物理地址转换速度的缓存。
   
2. 22年期末

   * 什么是虚拟存储器？为什么要在存储管理中引入虚拟存储器？

     > 虚拟存储器由内存和外存组成，使得程序的部分装入内存就能运行的技术，引入目的有二
     >
     > 1. 大作业能够运行
     > 2. 提高内存利用率

### 王道错题

1. ![image-20240311110119585](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311110119585.png)

   > B 对于**顺序执行程序，缺页次数等于其访问的页帧数**。由于**页面尺寸放大，存放程序需要的页帧数就减少，因此缺页中断次数也会减少**。

2. ![image-20240311110133608](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311110133608.png)

   > B **缺页中断是访存指令引起的**，说明要访问的页面不在内存中，缺页调入后，访存指令应该重新执行。

3. ![image-20240311110154410](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311110154410.png)

   > C 每种页面的第一次访问时不可能在内存中，必然发生缺页。
   >
   > 我的想法：我还以为是最少缺页次数

4. ![image-20240311110203357](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311110203357.png)

   > D FIFO有Belady异常，并且对于页面序列1，2，3，1，2，3当帧数为2时6次缺页，帧数为3时，3次缺页。

5. ![image-20240311110215145](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311110215145.png)

   > **D 虚拟存储器的最大容量是由计算机的地址结构决定的，与主存容量和外存容量没有必然的联系。**

6. ![image-20240311111642517](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311111642517.png)

   > **D LRU需要对页进行排序，A可视为耗费高，是因为D才需要硬件的支持。**

7. ![image-20240311111652586](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311111652586.png)

   > **A 页表项的合法位信息显示本页面是否在内存中，即决定了是否会发生页面故障。**

8. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311111703768.png" alt="image-20240311111703768" style="zoom:67%;" />

   > D 抖动是进程的页面置换过程中，频繁地页面调度行为，**所有页面调度策略都不可能完全避免抖动**

9. ![image-20240311112146393](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311112146393.png)

   > D 
   >
   > 我的想法：既然所有的页面调度算法都不能避免抖动，那为什么抖动的主要原因是页面置换算法不合理而不是内存空间太小？
   >
   > Bing：抖动现象通常是由于**进程频繁访问的页面数目高于可用的物理块数，导致进程分配的物理块太少而发生的。如果为进程分配的物理块过少，就会发生抖动现象**，因为系统需要不断地将页面在内存和外存之间调度，以至于调度页面所需的时间比进程实际运行的时间还多。**虽然内存空间的大小确实会影响到页面调度的效率，但页面置换算法的合理性对于减少抖动现象的发生更为关键。**合理的页面置换算法可以更有效地预测哪些页面将被频繁访问，从而减少不必要的页面调度，提高系统效率

10. ![image-20240311112157235](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311112157235.png)

    > C 虚拟存储技术是基于页或段从内存调入或调出实现的，**需要有请求机制的支持**

11. ![image-20240311113003580](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113003580.png)

    > B 虚拟内存的实现只能建立在离散分配的内存管理的基础上。三种实现方式：请求分页、请求分段、请求段页式；
    >
    > **虚存的实际容量受到外存和内存容量之和的限制，虚存的最大容量是由计算机的地址位数决定的**

12. ![image-20240311113012107](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113012107.png)

    > **C 让页表常驻内存可以省去一些不在内存中的页表从磁盘上调入的过程，也能加快虚实地址的转换；**

13. ![image-20240311113033418](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113033418.png)

    > C 对各个进程进行固定分配时页面数不变，不可能出现全局置换。
    >
    > **此处建议复习分配方式、置换方式，P164、196**

14. ![image-20240311113107339](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113107339.png)

    > D I影响缺页中断的频率，缺页率越高，平均访存时间越长。
    >
    > II、IV影响缺页中断的处理时间，中断处理时间越长，平均访存时间越长
    >
    > III影响访问页表和访问目标物理地址的时间

15. ![image-20240311113117568](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113117568.png)

    > C 虚拟地址位02A01H，页号为02，02对应的存在位为0，进程P分配的页框数是2，且已有两个页面存在内存中，根据Clock置换算法，**将3号页面置换出，将2号页放入60H页框，得到物理地址60A01H**

16. ![image-20240311113128282](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240311113128282.png)

    > C 对于A，整个过程完全由操作系统完成，不涉及用户，不是系统调用；
    >
    > 对于B，进程调度也完全由操作系统完成
    >
    > 对于D，生成随机数只需要普通函数调用，用不着系统调用。

# 第四章 文件管理

## 4.1 文件系统基础

### 研梦错题

1. *17年期末

   ![image-20240314115703509](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314115703509.png)

   > 答案未定，

2. 17年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314213808429.png" alt="image-20240314213808429" style="zoom:80%;" />

   > <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314213830097.png" alt="image-20240314213830097" style="zoom:80%;" />
   >
   > **系统打开文件表包含:FCB的副本及其他信息；还为每个文件关联一个打开计数器**
   >
   > **进程的文件打开表：根据其打开的所有文件，包含系统表中适当条目的指针，即文件描述符；**
   >
   > **FCB包括：文件时间、文件权限(创建、访问、写)、文件所有者、文件大小、文件数据块。**
   >
   > 索引文件的i结点，除了文件名，其他都有
   
3. 19年期末

   * 安全目标包括 **数据机密性、数据完整性、数据安全性、系统可用性**

### 王道错题

1. ![image-20240308193310371](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308193310371.png)

   > D UNIX系统中，所有设备都被视为特殊文件，因为UNIX操作系统控制和访问外部设备的方式和访问一个文件的方式是一样的

2. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308193419611.png" alt="image-20240308193419611" style="zoom: 80%;" />

   > A 打开文件的操作是将该文件的FCB放入内存的活跃文件目录表，而不是将文件内容复制到贮存；找到指定文件目录是打开文件之前的操作
   >
   > **此处建议复习打开文件相关内容，P231**

3. ![image-20240308193609582](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308193609582.png)

   > C 文件目录项即FCB，通常由基本信息、存储控制信息、使用信息组成。基本信息包括文件物理位置。FCB显然不包括FCB的的物理位置。
   >
   > 我的理解：文件目录项就是FCB，FCB不包含自己的位置，FCB在目录文件中
   >
   > **此处复习FCB相关内容，P230**

4. ![image-20240308194215465](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308194215465.png)

   > A 用户访问权限是指用户有没有权限访问该文件，用户优先级是指在多个用户同时请求该文件是应该先满足谁。
   >
   > 文件属性包括保存在FCB中对文件访问的控制信息，由控制信息和用户访问权限共同控制文件的访问

5. ![image-20240308194435614](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308194435614.png)

   > D 相对于加密保护机制，访问控制机制的安全性较差。因为访问控制的级别和保护力度较小，因此它的灵活性相对较高。若访问控制不由系统实现，则系统本身的安全性就无法保证。加密机制若由系统实现，则加密方法将无法扩展。

6. ![image-20240308194451384](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308194451384.png)

   > A 系统级安全管理包括注册和登录。

7. ![image-20240308201915795](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308201915795.png)

   > D 文件逻辑结构是用户组织数据的结构形式，数据组织形式来自需求。
   >
   > 因此说，逻辑文件的组织形式取决于用户，物理结构的选择取决于文件系统设计者对硬件接哦古所采取的策略（即A、B选项）；

8. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308201934552.png" alt="image-20240308201934552" style="zoom:67%;" />

   > B 索引文件由逻辑文件和索引表组成。文件的逻辑结构和物理结构都有索引的概念，引入逻辑索引和物理索引的目的不同。
   >
   > 逻辑索引的目的是加快文件数据的定位，是从用户角度出发的；
   >
   > 物理索引的主要目的是管理不连续的物理块，是从系统管理角度出发的；
   >
   > **此处建议复习逻辑、物理索引结构相关，P234、237**

9. ![image-20240308203037627](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203037627.png)

   > C 采用索引顺序文件时，最好的情况是有$\sqrt{10000} = 100$组，每组有100个记录，则查找100组平均需要(1+100)/2=50.5此，组内也是一样，所以共需要50.5+50.5=101次

10. ![image-20240308201944677](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308201944677.png)

    > B 直接存取即随机存取，采用连续分配和索引分配的文件都适合于直接存取方式，只有采用链接分配的文件不具有随机存取特性。
    >
    > 我的想法：通常来说链接分配方式是不支持直接存取的，但是FAT是一个例外

11. ![image-20240308203303835](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203303835.png)

    > D 第22个逻辑记录在第五个物理块中，22*100/512 = 4余152。由于采用链接分配方式，所以从第一个块开始读，共启动磁盘5次。修改后还要写入，由于写回时已获得该块的物理地址，所以只需要启动磁盘1次，共6次。

12. ![image-20240308203339843](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203339843.png)

    > B 索引表的表项中含有相应记录的关键字和存放该记录的逻辑地址；
    >
    > 对于三级索引，需要访问4次磁盘
    >
    > 无论顺序存取还是随机存取，顺序文件通常都是速度最快的。
    >
    > **此处建议复习索引文件相关，P234**

13. ![image-20240308203401460](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203401460.png)

    > A 对于I，当所读文件的数据不在内存时，产生缺页中断，原进程进入阻塞态，知道所需数据从外存调入才唤醒。
    >
    > 对于II，read系统调用通过陷入将CPU从用户态切换到核心态，从而获取操作系统提供的服务。
    >
    > 对于III，要读一个文件，首先要用open系统调用打开该文件。open中的参数包含文件的路径名和文件名，而read只需要使用open返回的文件描述符，并不使用文件名作为参数。read要求用户传入三个参数：文件描述符fd、buf缓冲区首地址、传送的字节数n。read的功能是从fd虽只是的文件中读入n个字节的数据，并将它们送至buf缓冲区

14. ![image-20240308203420913](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203420913.png)

    > D 可以把用户访问权限抽象成一个矩阵，行代表用户，列代表访问权限，这个矩阵有4行5列，1表示true，0表示false，所以需要20位

15. ![image-20240308203430114](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308203430114.png)

    > D 对于I，提前读是指在读当前盘块时，将下一个可能要访问的盘块数据读入缓冲区。
    >
    > 对于III，延迟写是指先将数据写入缓冲区，并置上“延迟写”的表示，以备不久后访问，当缓冲区需要再次被分配出去时，才将缓冲区数据写入磁盘，减少了访问磁盘的次数，提高文件访问速度。

## 4.2  目录

### 研梦错题

1. *17年期末

   ![image-20240314112007873](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314112007873.png)

   > 两级目录结构提高了检索速度、解决了多用户之间的文件重命名问题。**D没错**

2. 17年期末

   ![image-20240314115736128](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314115736128.png)

   > [在NTFS、FAT、EXT2、HPFS这几个文件系统中，FAT文件系统是不能实现文件别名机制的。文件别名机制，也被称为硬链接或软链接，允许一个文件或目录在文件系统中有多个有效路径名。NTFS、EXT2和HPFS都支持这种机制，但FAT由于其较早的设计和简单性，不支持文件别名](https://learn.microsoft.com/zh-cn/troubleshoot/windows-client/backup-and-storage/fat-hpfs-and-ntfs-file-systems)[1](https://learn.microsoft.com/zh-cn/troubleshoot/windows-client/backup-and-storage/fat-hpfs-and-ntfs-file-systems)。           

3. 文件系统为了实现按名存功能，通过 **文件目录表** 实现

4. 19年期末

   * 解释什么是 文件目录 和 目录文件？ UNIX系统的文件目录结构是如何组织的？
     1. **文件目录：为实现按名存取，必须建立文件名和辅存空间中物理地址的对应关系，体现这种对应关系的数据结构称为文件目录。**
     2. **目录文件：为了实现文件目录的管理，通常将文件目录以文件形式保存在外存空间，这个文件就被称为目录文件。目录文件是长度固定的记录式文件。**
     3. **所有的文件和目录都被组织成一个根节点开始的倒置的树状结构。文件系统的最顶层是由根目录开始的，系统使用/来表示根目录**。在根目录下的既可以是目录，也可以是文件，而每一个目录中又可以包含子目录文件。如此反复构成一个庞大的文件系统。
   
5. 22年期末

   * <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240320105423444.png" alt="image-20240320105423444" style="zoom:80%;" />

     > [在文件系统中，能够通过多条路径访问同一文件或目录的结构被称为**非循环图目录结构**。这种结构允许一个文件或目录在多个父目录中占有项目，但不构成环路，从而便于文件共享。非循环图目录结构通过链接（可以是硬链接或软链接）实现，使得同一文件或目录可以在文件系统中的不同位置被访问，而不需要复制文件本身](https://blog.csdn.net/weixin_43557093/article/details/103107061)。
     >
     > 这种结构的优点是提高了文件的可访问性和灵活性，特别是在需要文件共享和多个用户协作的环境中非常有用。例如，多个用户可以通过不同的路径访问同一份数据，而不必在每个用户的目录下都存储一个副本。此外，非循环图目录结构也支持文件的版本控制和权限管理，使得文件系统的维护更加高效和安全。

### 王道错题

1. ![image-20240308205700477](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308205700477.png)

   > C 共3200个目录项，查找一个文件平均需要查找1600个目录项，一个盘块可以有1024/64=16个目录项，因此平均需要1600/16次访问磁盘

2. ![image-20240308205826371](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308205826371.png)

   > C 实现用户对文件的按名存取，系统先利用用户提供的文件名形成检索路径，对目录进行检索。在顺序检索中，路径名的一个分量未找到，说明路径名中的某个目录或者文件不存在，不需要继续检索。
   >
   > 目录的查询方式有两种，顺序检索法和Hash法，通常采用顺序检索法。至于顺序检索法和Hash法的速度，看数据规模，在小数据规模的情况下，顺序检索法也不慢。
   >
   > 在树形目录中，为了加快检索速度，可设置当前目录，于是文件路径可以从当前的目录开始查找
   >
   > 在顺序检索法查找完成后，得到的是文件的逻辑地址
   >
   > **此处建议复习检索方式相关，P254、256**

3. ![image-20240308210000063](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308210000063.png)

   > C 文件系统中采取多级目录结构后，符合了多层次管理的需要，提高了文件查找速度，还允许用户建立同名文件。所以，多级目录结构解决了命名冲突。

4. ![image-20240308211319685](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308211319685.png)

   > D 对于IV，为防止文件受损常采用备份的方法，而存取控制矩阵方法用于多用户之间的存取权限保护

5. ![image-20240308211329464](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308211329464.png)

   > B 建立符号链接时，引用计数值直接复制；建立硬链接时，引用计数值+1。删除文件时，删除操作对于符号链接是不可见的；但对于硬链接不可直接删除，引用计数值-1，若值不为0，则不能删除此文件。

6. ![image-20240308211344997](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308211344997.png)

   > B 硬链接指通过索引节点进行链接。一个文件在物理存储器上有一个索引节点号，存在多个文件名指向同一个索引节点的情况。II正确
   >
   > 两个进程各自维护自己的文件描述符，III正确；指向同一个的是系统文件打开表的一个表目，是一个FCB副本

7. ![image-20240308211355680](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308211355680.png)

   > B 多个进程可以同时以读、写方式打开文件，操作系统并不保证写操作的互斥性，进程课通过系统调用对文件加锁，保证互斥写
   >
   > 整个系统只有一个系统打开文件表，同一个文件打开多次只改变引用计数
   >
   > 用户进程的打开文件表关于同一个文件不一定相同，例如读写指针的位置
   >
   > 进程关闭文件时，文件的引用计数-1，引用计数为0时才删除系统打开文件表的表项。

## 4.3 文件系统

### 研梦错题

1. 在UNIX系统中，采用 **成组链接法** 对空闲磁盘块进行管理

2. 18年期末

   **位示图只能用在磁盘管理**

3. 18年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316115510373.png" alt="image-20240316115510373" style="zoom:80%;" />

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316115540196.png" alt="image-20240316115540196" style="zoom: 67%;" />

   > 1. 补充：
   >
   >    ![image-20240316144311699](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316144311699.png)
   >
   >    每组的第一块是成组链块即超级块，超级块的第一项代表下一组的空闲盘块数量，第二项就是下一组的超级块编号，其余项是下一组的各个空闲块编号。当没下一组没有超级块了即下一组是最后一组了那本组超级块的第二项就标为-1；
   >
   >    ![image-20240316144908032](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316144908032.png)
   >
   >    分配空闲块时，从下一组的最后一块开始分配，分配完毕后将被分片出去的块号在本组的超级块中删除；如果要将下一组全部分配出去，那么要将下一组的超级块中的信息复制到本超级块中；
   >
   >    ![image-20240316145300513](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316145300513.png)
   >
   >    若本来的第一个分组(400号组)已经满了，那么将新收回的块作为新组的第一块，创建一组，将新组的超级块指向原本的第一组(即300号块指向400号块)，然后将系统第一块超级块指向新组的超级块。
   >
   >    接下来，下面的答案应该就能看懂了;但是似乎结构与王道不太一致
   >
   > 2. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316115614650.png" alt="image-20240316115614650" style="zoom:67%;" />
   >
   > 3. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316115629419.png" alt="image-20240316115629419" style="zoom:67%;" />
   >
   >    

### 王道错题

1. ![image-20240308212916719](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308212916719.png)

   > B UINX采用树形目录结构，文件信息存放在索引结点中。超级块是用来描述文件系统的。
   >
   > **此处建议复习超级块相关，P269，270**

2. ![image-20240308212928464](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308212928464.png)

   > D 注意，本题的下标都是从1开始的，而不是0；王道书中，默认从1开始

3. ![image-20240308212944026](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308212944026.png)

   > C 每个盘块占1024B，即8192个位，409612/8192 = 50余12；故32+50=82，12/8=1余2，又块内字节用0开始编号，所以第2块的块号是1

4. ![image-20240308212952127](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308212952127.png)

   > B 传统文件系统管理空闲磁盘的方法包括：空闲表法、空闲链表法、位示图法和成组链接法
   >
   > 文件分配表FAT的表项和物理磁盘块一一对应，并且可以用一个特殊数字-1表示文件的最后一个块，用-2表示这个磁盘块是空闲的，因此FAT不仅记录了文件中各个块的先后链接关系，还同时标记了空闲磁盘块，操作系统可以通过FAT对文件存储空间进行管理。
   >
   > 索引结点是为了实现文件名和文件信息分开而设计的数据结构，存储了文件描述信息，索引结点属于文件目录管理部分的内容。

# 第五章 IO管理

## 5.1 IO管理概述

### 研梦错题

1. 17年期末

   ![image-20240314115534372](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314115534372.png)

   > C设备开关表包括：设备控制表DCT、控制器控制表COCT、通道控制表CHCT、系统设备表SDT
   >
   > **系统调用应当是在文件系统和应用进程之间的**
   
2. 19年期末

   * 程序控制IO和中断控制IO的区别
     1. 程序控制IO：让CPU做全部的工作。CPU通过程序主动读取状态寄存器以了解接口情况，并完成相应的数据操作
     2. 中断控制IO：在等待设备就绪的时候允许CPU做其他的事情。当程序常规运行时，若外部由优先级更高的事件出现，则通过中断请求通知CPU，CPU再读取状态寄存器确定事件的种类，执行不同的分支处理。

3. 20年期末

   * **数组选择通道不能支持多个通道程序并发执行，而字节多路通道可以支持多个通道程序并发执行**

     > 数组**选择**通道（Array Selective Channel）**在某一时间段内只能选择一个设备进行工作，因此它不支持多个通道程序的并发执行**。而字节**多路**通道（Byte Multiplexor Channel）**含有多个非分配型子通道，每个子通道连接一台I/O设备，并且可以以时间片轮转方式共享主通道，从而支持多个通道程序的并发执行**。
     >
     > 简单来说，数组选择通道适合于连接高速外围设备，如磁盘、磁带等，它们在数据传输期间占用通道，直到传输完成。而字节多路通道则适用于低速或中速I/O设备，可以在短时间内交替服务多个设备，实现并发操作。数组**多路**通道（Array Multiplexor Channel）则结合了两者的优点，**能够在控制性操作时为其他设备提供服务，从而提高通道利用率和并发性。**         

### 王道错题

1. 关于设备属性的叙述中

   * A：字符设备的特征是 **信息交换以字符为单位，无结构类型，传输速率低，不可寻址，采用中断IO方式**
   * 共享设备 **是指一段时间内允许多个进程同时访问的设备**
   * 分配共享设备是不会引起进程死锁的

2. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095133365.png" alt="image-20240308095133365" style="zoom: 67%;" />

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095034543.png" alt="image-20240308095034543" style="zoom:80%;" />

   CPU通过控制命令控制设备

3. ![image-20240308095310995](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095310995.png)

   **C** 接口用于传输信号，IO逻辑即设备控制器，用以实现对设备的控制

4. ![image-20240308095410617](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095410617.png)

   **A**

   * 设备固有属性决定了设备的使用方式
   * 设备独立性可以提高设备分配的灵活性和设备利用率
   * 设备安全性可以保证分配设备时不会导致永久阻塞

   **设备分配一般不考虑及时性**

5. ![image-20240308095600718](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095600718.png)

   **A** 计算机系统为每台设备确定一个编号以便区分和识别设备，编号称为 **设备的绝对号**

6. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095706581.png" alt="image-20240308095706581" style="zoom: 67%;" />

   三者的控制关系的层层递进的，**C是正确的**

7. ![image-20240308095800374](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095800374.png)

   **C** 绘图机和打印机属于不同类型的设备，**系统只要按照设备类型配置设备驱动程序即可，每类设备只需提供一个设备驱动程序**

8. ![image-20240308095922387](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308095922387.png)

   系统调用是操作系统提供给用户的通用接口，属于设备无关软件的功能，不会因为设备改变而不同。设备驱动程序负责执行操作系统发出的IO命令。**B**

   > 1. 用户层IO：实现与用户交互的接口，用户层必须通过一组系统调用来获取操作系统服务
   > 2. 设备独立性软件：实现用户程序与设备驱动器的统一接口、设备命令、设备保护、设备分配与释放等
   > 3. 设备驱动程序：负责具体实现系统对设备发出的操作指令，每类设备配置一个驱动程序
   > 4. 中断处理程序：进行上下文切换、对处理中断信号源进行测试、读取设备状态和修改进程状态

9. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308100614076.png" alt="image-20240308100614076" style="zoom: 67%;" />

   **B** 开始DMA传输时，主机箱内存写入DMA命令块，向DMA控制器写入该命令块的地址，启动IO设备；CPU继续其他工作，DMA控制器则继续直接操作内存总线，将地址放到总线上开始传输。整个传输完成后，DMA控制器中断CPU。

## 5.2 设备独立性软件

### 研梦错题

1. 设备独立性是指：**用户在编程时所使用的设备与物理设备无关**

2. 设备管理中引入缓冲的原因：**解决CPU与IO设备间速度不匹配的问题**，从而提高CPU、通道、IO设备之间的并行性，减少CPU被中断的次数

3. *18年期末

   <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240316151152119.png" alt="image-20240316151152119" style="zoom: 67%;" />

   > A没错
   
4. 18年期末

   缓冲区的作用：

   * **缓和CPU和IO设备之间速率不匹配的问题**
   * **减少对CPU的中断频率，放宽对中断响应时间的限制**
   * **提高CPU和IO设备之间的并行性**

   UNIX为块设备设置缓冲区的目的是：**提高基本速率相差较大的块设备之间的吞吐量，并减少对CPU的中断次数**
   
5. 22年期末

   * 什么是设备无关性？有什么好处？

     > * 基本含义：应用程序独立于具体使用的物理设备。为了实现设备独立性而引入了逻辑设备和物理设备两个概念。在应用程序中，使用逻辑设备名称来请求使用某类设备；而系统在实际执行时，还必须使用物理名称
     > * 优点：设备分配时的灵活性、易于实现IO重定向，而不必改变应用程序

### 王道错题

1. ![image-20240308104343374](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308104343374.png)

   **C** 通过高速缓冲存技术改善两者速度不匹配的问题

2. ![image-20240308104438871](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308104438871.png)

   **A** 缓冲池是系统的共用资源，可以多个进程共享，并且既可以用于输出又可以用于输入。

   **其他三者属于专用缓冲**

3. ![image-20240308104558508](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308104558508.png)

   **A** 

4. ![image-20240308104700834](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308104700834.png)

   > A
   >
   > 采用单缓冲区传送数据时，设备与处理机对缓冲区的操作是串行的，当进行第i次读磁盘数据送至缓冲区时，系统再同时读出第i-1次数据进行计算，此两项操作可以并行。
   >
   > 我的理解，本题主要误解就在于这如果是连续的几块数据处理，那么当i-1块在计算时，第i块已经在从磁盘传送到缓冲区了，这样计算与传输时间就覆盖了，只要取两者最大值就行，最后再加上缓冲到用户的时间；
   >
   > 但是如果只有一块数据的话，那就要加上全程的了，因为在计算时，并没有下一块数据在传送

5. ![image-20240308105358278](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308105358278.png)

   > D 本来的公式是$max\{C+M,T\}$,其中C是计算时间，M是缓冲到用户时间，T是磁盘到缓冲时间，本题说M远小于T，那么可以将M忽略不计，就是$max\{C,T\}$

6. ![image-20240308105722376](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308105722376.png)

   > B **缓冲区主要解决IO速度比CPU处理速度慢的问题，所以当IO时间比CPU处理时间短时，缓冲没必要设置**

7. ![image-20240308105733720](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308105733720.png)

   > C 缓冲机制中，无论是什么缓冲，由于缓冲是一种临界资源，所以在使用缓冲时都有一个互斥问题要考虑

8. ![image-20240308105747901](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308105747901.png)

   > D 在鼠标移动时，如果有高优先级的操作产生，为了记录鼠标的活动，必须使用缓冲。

9. ![image-20240308110156001](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308110156001.png)

   > C 磁带机旋转到所需读写的位置需要较长时间，如果一个时间间隔内被多个进程访问，磁带机就只能一直旋转，没时间读写

10. ![image-20240308110205813](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308110205813.png)

    > D 在单机系统中， 最关键的资源就是处理器资源，最大化提高处理器利用率就是最大化提高系统效率，所以是多道程序设计技术

11. ![image-20240308110531082](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308110531082.png)

    > A 此处建议去看看SPOOLing技术的运行过程
    >
    > 假脱机管理程序为用户进程在外存输出井中申请一片区域，用户进程的数据先到外存的输出井中；待外设运行时，从输出井中取一个任务，将输出井中的输出放到输出缓冲区，然后交给外设；外设处理完毕后将结果放到输入井中，待用户进程需要，将输入井中的数据放到输入缓冲区，然后交给用户进程

12. ![image-20240308110541359](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308110541359.png)

    > D SPOOLing技术需要使用磁盘空间（输入输出井）和内存空间（输入输出缓冲区），不需要外围计算机的支持

13. ![image-20240308110842352](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308110842352.png)

    > D SPOOLing技术基本条件：大容量、高速度的外存作为输入输出井，而且还要有SPOOLing软件； 利用SPOOLing技术，进程不必等待IO操作完成

14. ![image-20240308111041576](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308111041576.png)

    > D 独占设备采用静态分配方式，共享设备采用动态分配方式
    >
    > 静态分配：在用户作业开始前一次性分配该作业所需的全部设备
    >
    > 动态分配：在进程的执行过程中动态分配设备

15. ![image-20240308111116492](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308111116492.png)

    > D SPOOLing技术需要进行输出输出操作，单道批处理无法完成
    >
    > 设备与输出输出井之间数据传送是由系统实现的

16. ![image-20240308111126833](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308111126833.png)

    > D 由于设备可以视为特殊文件，所以A正确

17. ![image-20240308111135472](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308111135472.png)

    > A 厂家会根据设备特性，在驱动程序中实现一种适合的IO控制方式
    >
    > 当一个设备连接到主机时，由设备驱动程序负责初始化设备
    >
    > 当进程在执行驱动程序时，可能会因为设备忙碌而进入阻塞态
    >
    > 设备的读写操作本质上就是在设备控制器和主机之间传送数据，而只有厂家知道设备控制器的内部实现，因此只有厂家提供的驱动程序能控制设备的读写操作



## 5.3 磁盘和固态硬盘

### 研梦错题

1. 17年期末![image-20240314121150219](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240314121150219.png)
   1. 处理完13个记录的平均时间是多少？
   2. 为缩短处理时间应该如何排列这些记录？重新计算排列后的处理时间。
   
   > 1. 392.7ms
   > 2. 118.1ms
   >
   > 无解释，本题建议参考王道P312第二大题，
   >
   > 1. 一圈30ms，共13条记录，30/13ms一条记录，读出R1后处理完R1是30/13ms+5ms，此时已经到了R4的中间位置，为了再次转到R2的位置，需要再转过11条记录，也就是从R4中间开始到R2开头的时间里，都没有操作，也就是从开始读取R1到转弯一圈到R2开头一共处理了一个记录，也就是说一圈+一个记录的时间才处理了一个记录；但是最后一个记录用再转了，所以只需要加上读取+处理时间；总共应该是
   >
   >    12*(30+30/13)+5+30/13=395ms
   >
   > 2. 同样的，排好序的情况下，应该是不用转的，顺序应该是：
   >
   >    1，11，8，5，2，12，9，6，3，13，10，7，4
   >
   >    刚好每个序号中间间隔了4个位置，因为7.3/2.3的上取整是4，从上图可以看出，1处理完时，指针已经到了R4的中间位置，所以2应该在R5位置，
   >
   >    所以总时间应该是前12个记录都需要整位置倍的移动时间也就是说从1号位到4号位结束共4个位置的旋转时间以可处理完一条记录，但是最后一个只需要一个读取+一个处理时间，
   >
   >    $12\times(\frac{30}{13} \times 4)+\frac{30}{13}+5=118.1ms$
   
2. 19年期末

   ![image-20240318112125248](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240318112125248.png)

   > Bing：
   >
   > 读取或写入磁盘所需的时间主要由三个因素决定：寻道时间、旋转延迟和传输时间。**在这三个因素中，寻道时间通常是主导因素。**寻道时间是磁头移动到指定磁道所需的时间，**由于它涉及磁头在磁盘表面上的机械运动，因此通常是最耗时的部分。**各种磁盘调度算法的目的都是为了优化寻道时间，以提高磁盘的性能。

### 王道错题

1. ![image-20240308115151900](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308115151900.png)

   > D 操作系统的引导程序位与磁盘活动分区的引导扇区，因此必然产生在分区之后。
   >
   > 每个分区的起始扇区和大小都记录在磁盘主引导记录的分区表中。
   >
   > 对于高级格式化（创建文件系统），操作系统将初始的文件系统数据结构存储到磁盘上
   >
   > **此处建议回顾低级格式化、高级格式化、操作系统的启动过程**

2. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308115202452.png" alt="image-20240308115202452" style="zoom:80%;" />

   > D 磁盘调度算法是为了减少寻道时间。扇区数据的处理时间主要影响传输时间。
   >
   > 磁盘空的分配程序、文件的物理结构都与旋转延迟的大小有关。
   >
   > 文件的物理结构和磁盘空间的分配方式对应。

3. ![image-20240308115807082](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308115807082.png)

   > A 在随即寻道的情况下，读写一个磁道的时间包括寻道时间和读写磁道时间，即T+r秒。由于总数据量是b字节，它要占用的磁道数为b/N个，所以总时间为(T+r)b/N

4. <img src="https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308115831851.png" alt="image-20240308115831851" style="zoom: 67%;" />

   > C SSD的写速度慢于读速度，但是不至于比机械硬盘差
   >
   > SSD基于闪存技术，没有机械部件所以随机存取速度快；传统机械硬盘转速很快，连续存取比随机存取快得多，因为SSD的优势主要体现在随机存取的速度上。
   >
   > 静态磨损算法在没有写入数据时，SSD监测并自动进行数据分配，因此表现更加优秀
   >
   > 因为闪存的擦除速度较慢，若每次都选择写入存放有数据的块，会降低写入速度
   >
   > D混淆了静态磨损均衡，静态磨损均衡是指在没有写入数据时，SSD监测并自动进行数据分配，从而使得各块的擦写更加均衡，并不是说写入时都选择存放老数据的块。

5. ![image-20240308120749255](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308120749255.png)

   > B 新磁盘是空白的，必须分成各个扇区以便磁盘控制器能读写，这个过程称为低级格式化(物理格式化)。
   >
   > 低级格式化为每个磁盘的每个扇区采用特别的数据结构，包括校验码。
   >
   > 为了使用磁盘存储文件，操作系统还要将自己的数据结构记录在磁盘上，分为两步：
   >
   > 1. 将磁盘分为一个或多个柱面组成的分区，每个分区可以作为一个独立的磁盘
   > 2. 逻辑格式化(创建文件系统)。操作系统将出事的文件系统数据结构存储到磁盘上，包括空闲和已分配的空间以及一个初始为空的目录。

6. ![image-20240308120759626](https://typora2icture.oss-cn-beijing.aliyuncs.com/Algorithm/image-20240308120759626.png)

   > A 当系统持续出现某个磁道的访问请求时，均持续满足SSTF、SCAN、CSCAN算法的访问条件，会一直服务该请求，而FCFS按照请求次序进行调度，较为公平。
   >
   > 我的理解：比如访问1，4，5，2，5，6，5，8，5，9，5如果此时磁头刚好在5号，那么后三个算法都将持续访问5号，但FCFS不会